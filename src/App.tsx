import React from 'react';
import {AppFrame} from "./panels/AppFrame";

const App: React.FC = () => {
    return (
        <div>
            <title>Active Tasks</title>
            <AppFrame/>
        </div>
    );
};

export default App;